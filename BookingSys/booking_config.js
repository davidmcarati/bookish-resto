const config = {
    db_auth     : {
        user        : "[username]",
        password    : "[password]",
        host        : "[hostname]",
        port        : "[port:num]",
        database    : "[database]"
    },
    day_start   : "07:00",
    day_end     : "24:00"
};

module.exports = config;