const config = {
    db_auth     : {
        user        : "[username]",
        password    : "[password]",
        host        : "[hostname]",
        port        : "[port:num]",
        database    : "[database]"
    }
};

module.exports = config;